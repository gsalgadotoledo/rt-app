# choice-transformers

Local zero-shot classification through an injected Transformers.js pipeline. Scores are uncalibrated.

See [configuration and examples](../../docs/decisions-and-queues.md).

```sh
npm run build
npm test
```
