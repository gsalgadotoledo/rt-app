# NoSQLCache

`new NoSQLCache(store, namespace)` implements get/set/delete with logical TTL and conditional writes. Shared JSON/DynamoDB contract.

See the repository `rt-app/docs/components.md` and `rt-app/docs/observer.md` for setup, limits and examples.

All rights reserved.
