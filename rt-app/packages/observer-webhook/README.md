# WebhookOutput

Custom HTTPS JSON output configured in trusted server code. Redirects are rejected. `new WebhookOutput(id, url, headers)`.

See the repository `rt-app/docs/components.md` and `rt-app/docs/observer.md` for setup, limits and examples.

All rights reserved.
