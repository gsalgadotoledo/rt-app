# Analytics

Product events over Observer. `analytics.track("checkout.completed", properties)` and `analytics.pageView(title, options)`. Browser page helper: `./browser`.

See the repository `rt-app/docs/components.md` and `rt-app/docs/observer.md` for setup, limits and examples.

All rights reserved.
