{
  "name": "rt-app-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "rt-app",
    "rt-app/packages/*",
    "rt-app/admin",
    "rt-app/cli",
    "rt-app/installer",
    "rt-app/core-ts"
  ],
  "scripts": {
    "build": "node rt-app/scripts/workspaces.mjs build && tsc -p templates/hello-world/apps/lambda-ts/tsconfig.json",
    "test": "npm test --workspaces --if-present",
    "check:architecture": "node rt-app/scripts/workspaces.mjs check",
    "prepare:templates": "node rt-app/packages/create/scripts/prepare.mjs",
    "release:pack": "node rt-app/scripts/release.mjs pack",
    "release:verify": "node rt-app/scripts/release.mjs verify",
    "release:publish": "node rt-app/scripts/release.mjs publish",
    "release:check": "npm run check:architecture && npm run build && npm run prepare:templates && npm test && npm run release:pack && npm run release:verify"
  },
  "engines": {
    "node": ">=22.14"
  },
  "overrides": {
    "postcss": "8.5.28"
  }
}
